WORKFLOW "Newman API Tests"

ON: pull_request, push(main), workflow_dispatch
PERMISSIONS: contents=read, checks=write, pull-requests=write

JOB test (ubuntu-latest):
  ENV: COLLECTION="postman/collection.json", ENVFILE="postman/environment.ci.json"

  STEPS:
    - Checkout repo
    - Setup Node.js 20
    - Install newman + reporter
    - Debug: check files, warn if "localhost"
    - Rewrite URLs → {{API_BASE_URL}}/path
    - Confirm: no "localhost", no spaced vars
    - Run newman tests with secrets (base url, email, password)
    - Upload reports (JUnit, HTML)
    - Publish JUnit summary on PR
